# PostgreSQL connection (matches docker-compose.yml)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/status_check?schema=public"

# Auth.js (NextAuth) secret used to sign sessions/JWTs.
# Generate one with: npx auth secret
AUTH_SECRET="replace-with-a-random-32-byte-secret"
